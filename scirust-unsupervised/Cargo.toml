[package]
name = "scirust-unsupervised"
version = "0.1.0"
edition = "2021"
description = "Unsupervised pattern detection: autoencoders, isolation forest, DBSCAN"
license = "PolyForm-Noncommercial-1.0.0"

[dependencies]
scirust-core = { path = "../scirust-core" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
