# V888-BOOL-0.1 — observed executable graph qualification

Observed on 2026-09-22. Author: Memorithm research pipeline.
Scope: exact source-graph qualification, not biological reconstruction, Boolean task performance or public-library promotion.

## Executed source and host

- Successful Thor-labelled CPU workflow: https://github.com/Memorithm/scirust/actions/runs/35717145115
- Executed commit: `d61ffb6a79ebe975c1d867ad1a728fd01126fde0`.
- Observed runner: `tarek-scirust-arm64-01`; hostname: `tarek`; architecture: `aarch64`.
- Compiler: Rust 1.89.0, `29483883e`, 2025-08-04.
- Rust worker SHA-256: `dbf349dafcde9b4967bb514e8875f375801a2994315126c5f09458e2f799ed02`.
- Protocol canonical SHA-256: `497cdcd57ac8c1d9052b8df6a0b1fb5b2839cdb61079ac4ad719d4f7bb080ad7`.

The first run, 35716816008, failed during checkout because an existing worktree artifact was not writable. No tests or raw scan ran in that attempt. Per-run checkout isolation resolved the infrastructure issue without deleting the existing artifact, changing global permissions or mutating the data.

## Exact directed-pair result

| Quantity | Observed value |
| --- | ---: |
| Metadata nodes retained | 188,508 |
| Published directed reference pairs | 13,620,865 |
| Raw endpoint records scanned | 198,816,365 |
| Contacts with both endpoints in metadata | 42,309,621 |
| Contacts with only presynaptic endpoint in metadata | 141,812,486 |
| Contacts with only postsynaptic endpoint in metadata | 14,694,258 |
| Contacts with neither endpoint in metadata | 0 |
| Reference pairs with a different raw multiplicity | **0** |
| Unexpected induced-pair records | **0** |
| Raw self-endpoint records | 0 |

This strengthens the earlier node-total audit: the induced raw-v3 multiset now agrees with the published v3 edgelist **pair by pair, with exact integer multiplicities**. The raw boundary records remain preserved and are not interpreted as a particular neuron type or reconstruction error.

The input files were rehashed and checked against the retained source identities and Google Cloud Storage size/MD5 metadata. Their SHA-256 values also agree with the previous Phase-0 manifest:

- metadata: `86ccf5df0c67419f8c5f43e93a7ed38d23a080e9f7fde26737290252f3780098`;
- edgelist: `8c296e946f3c69a8c7222f30ad75fa8a98eeb189124fec6df829c9125f4be64b`;
- raw synapses: `0dfb5cf89ba156d076beab2da38d87eaa63dcbe45d76f86b108570fb5b961dd0`.

The complete raw Parquet remains 19,733,123,829 bytes and was not modified, deleted or uploaded as a repository/workflow artifact.

## Canonical graph emitted

The `V8CSR001` graph contains sorted original u64 root IDs, outgoing u64 row offsets, sorted u32 target indices and u64 contact multiplicities. The independent Python readback passed byte-length, index, row-order and total-weight checks.

- Graph bytes: **166,466,540**.
- Graph SHA-256: `385111a69cc8a1d748c0bdfd9b0b738c51fe83cde98f45762553435a2d15a2a4`.
- Node-map SHA-256: `eaa481b3aef42f63fb9e2e8f9c27d1405c073c54d11cbcd656ca9432904f425a`.
- Canonical graph/source/filter identity: `3cfbed6392a097865147ac08fae65f36a986b2ba6d386b1f5d61b9cfbefd1dd9`.
- Logical host path: `$HOME/datasets/banc_v888/analysis/bool01-35717145115-1/graph.csr`.
- Resolved path recorded by the process: `/ota_keep/mnt-nvme-r38.4/github-runners/home/datasets/banc_v888/analysis/bool01-35717145115-1/graph.csr`.

The smaller graph is a different representation and source scope from the complete raw table, not a lossless compression of all raw synapse metadata. It is ready as an explicitly identified input for subset/control work; it does not yet supply recurrent neuronal dynamics.

## Software evidence and resource observations

Seven Rust tests and eight independent Python/process/Arrow test methods passed. The latter include twelve seeded multiset experiments with Counter references, byte-identical canonical output after source reordering, incorrect-pair controls preserving node totals, large integer IDs, malformed streams and independent binary readback.

The measured qualification interval was **234.63493113406003 seconds**, including source verification, decoding, exact counting, graph output and independent readback. Rust child maximum RSS was **645,932 KiB**; Python maximum RSS was **761,924 KiB**. These are separate process peaks, not a simultaneous whole-machine peak, energy measurement or comparative speedup. The earlier configured 8 GiB address-space ceiling is not measured RAM consumption.

## Retained artifact and independent retrieval checks

Artifact **10689412746**, `v888-bool01-d61ffb6a79ebe975c1d867ad1a728fd01126fde0`, is retained by run 35717145115. ZIP SHA-256:

`f6758eb1ebc8e540703134433804b3317502222abcff99509c11b1cc053368f3`

The ZIP digest, graph-contract canonical fingerprint and protocol canonical fingerprint were independently recomputed after retrieving the compact evidence. The ZIP contains the source identities, pair counts, graph identity, runtime/environment and test logs, not the raw data or graph binary. These are reproducibility identities, not signed build attestations.

## Programme transition and remaining boundaries

V888-BOOL-0.1 is completed at source/software qualification scope. V888-BOOL-0.2, deterministic subsets and topology controls, is the next implementation. Delayed XOR remains the first functional target after dynamics and input/readout gates.

No synapse-ID uniqueness, physiological conductance/sign/delay, Boolean task success, topology advantage, biological growth cause, GPU acceleration or model-default change is established. Code remains on PR #1505, stacked on #1502; a successful research run is not a merge into master. Existing ITD V29.18 and all final-holdout/actuation/merge-authority boundaries remain unchanged.
