[package]
name = "scirust-tensor-search-bridge"
version = "0.1.0"
edition = "2024"
publish = false
rust-version = "1.89"
description = "Verify-before-measure bridge between canonical Tensor IR candidates and SciRust optimization runtimes"
license-file = "../LICENSE.md"

[dependencies]
scirust-opt-core = { path = "../scirust-opt-core" }
scirust-tensor-ir = { path = "../scirust-tensor-ir" }
