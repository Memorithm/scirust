[package]
name = "sos-workflow"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "SOS Workflow Engine (deterministic core): a build system for knowledge — immutable stage DAGs, content-addressed cache keys, deterministic memoized scheduling, and a RunLedger recording the schedule taken. Pure Rust, no FFI."
keywords = ["workflow", "scheduler", "memoization", "deterministic", "science"]
categories = ["science", "data-structures"]

[dependencies]
sos-core = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
sos-store = { workspace = true }
serde_json = { workspace = true }
