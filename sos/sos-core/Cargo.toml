[package]
name = "sos-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Scientific Operating System (SOS) kernel: immutable, content-addressed scientific objects with deterministic canonical hashing, an honest determinism-level taxonomy, and full provenance/reproducibility metadata. Pure Rust, no FFI, no unsafe."
keywords = ["science", "reproducibility", "provenance", "content-addressed", "deterministic"]
categories = ["science", "data-structures"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }
