[package]
name = "scirust-gpu"
version = "0.1.0"
edition = "2024"

[dependencies]
rayon = { version = "1.10", optional = true }
cudarc = { version = "0.14", optional = true, features = ["driver"] }

[features]
default = ["cpu-fallback"]
cpu-fallback = ["rayon"]
cuda = ["dep:cudarc"]
wgpu = []
