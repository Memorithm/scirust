[package]
name = "scirust-gpu"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = ["std"]
std = []
cuda = []
wgpu = []

[dependencies]
scirust-gpu-macros = { path = "../scirust-gpu-macros" }
