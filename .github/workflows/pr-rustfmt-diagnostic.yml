name: PR rustfmt diagnostic

on:
  pull_request:
    branches: [master]

permissions:
  contents: read

jobs:
  rustfmt-diagnostic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Produce rustfmt patch
        run: |
          set -euo pipefail
          cargo fmt --all
          git diff -- scirust-core/src/amp.rs > rustfmt.patch
      - name: Upload rustfmt patch
        uses: actions/upload-artifact@v4
        with:
          name: scirust-amp-rustfmt-${{ github.event.pull_request.head.sha }}
          path: rustfmt.patch
          if-no-files-found: error
          retention-days: 1
