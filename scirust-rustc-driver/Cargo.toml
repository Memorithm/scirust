[package]
name = "scirust-rustc-driver"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
authors = ["SciRust Team"]
description = "Custom rustc driver for SciRust scientific computing extensions"
publish = false

# This crate uses rustc internal crates (rustc_driver, rustc_interface, etc.)
# which are only available on nightly with the `rustc-dev` component installed.
# We use `rustc_private` feature and `--extern` flags via .cargo/config.toml.
