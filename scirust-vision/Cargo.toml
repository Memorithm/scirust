[package]
name = "scirust-vision"
version = "0.1.0"
edition = "2021"
description = "Computer vision patterns: CNN, object detection, image classification, segmentation"
license = "PolyForm-Noncommercial-1.0.0"

[dependencies]
scirust-core = { path = "../scirust-core" }
scirust-signal = { path = "../scirust-signal" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
