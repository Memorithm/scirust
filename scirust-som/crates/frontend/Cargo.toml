[package]
name = "scirust-som-frontend"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
scirust-som-pcg = { path = "../pcg" }
syn = { version = "2", features = ["full", "parsing"] }
