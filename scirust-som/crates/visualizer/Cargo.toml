[package]
name = "scirust-som-visualizer"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
scirust-som-symbolic = { path = "../symbolic" }
scirust-som-tokenizer = { path = "../tokenizer" }

[dev-dependencies]
scirust-som-pcg = { path = "../pcg" }
