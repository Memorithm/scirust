[package]
name = "scirust-som-dataset"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
scirust-som-pcg = { path = "../pcg" }
scirust-som-tokenizer = { path = "../tokenizer" }
scirust-som-symbolic = { path = "../symbolic" }
scirust-core = { path = "../../../scirust-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
