[package]
name = "scirust-nas"
version = "0.1.0"
edition = "2021"
publish = false
description = "Neural Architecture Search — evolutionary and gradient-based architecture optimization"

[dependencies]
scirust-core = { path = "../scirust-core" }
scirust-evo = { path = "../scirust-evo" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
