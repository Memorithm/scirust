//! Print the reproducible VST-selection benchmark table (CANR §9 "benchmark
//! tool"). Regenerate the committed artifact with:
//!
//! ```text
//! cargo run -p scirust-signal --example vst_benchmark > docs/research/vst_benchmark_table.md
//! ```
use scirust_signal::denoise::vst_bench::vst_benchmark_default;

fn main() {
    let table = vst_benchmark_default();
    println!("<!-- generated by `cargo run -p scirust-signal --example vst_benchmark` -->");
    println!("# VST-selection benchmark (CANR §9)\n");
    println!(
        "Data-driven VST autotuning ({}), 1024-sample smooth positive reference,\n\
         dev seed 1 / held-out seed 2, default VST candidate set. `gain` is the\n\
         held-out SNR of the empirically-chosen transform minus direct denoising.\n",
        "select on dev, validate on held-out"
    );
    print!("{}", table.to_markdown());
}
