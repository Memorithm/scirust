[package]
name = "scirust-onnx"
version = "0.1.0"
edition = "2021"
publish = false
description = "ONNX model export for SciRust — convert trained models to standard ONNX format"

[dependencies]
scirust-core = { path = "../scirust-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
