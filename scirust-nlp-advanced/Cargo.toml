[package]
name = "scirust-nlp-advanced"
version = "0.1.0"
edition = "2021"
description = "Advanced NLP patterns: NER, topic modeling, relation extraction, text classification"
license = "PolyForm-Noncommercial-1.0.0"

[dependencies]
scirust-core = { path = "../scirust-core" }
scirust-learning = { path = "../scirust-learning" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
